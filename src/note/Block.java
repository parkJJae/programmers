package note;

public class Block {
    public int solution(String[][] board, int h, int w) {
        int answer = 0;
        int n = board.length;
        int count = 0; // 같은 색으로 색칠된 칸의 개수 저장

        return answer;
    }
}
